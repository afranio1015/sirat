<!DOCTYPE html>
<html lang="pt_BR">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <?php
        echo "<meta HTTP-EQUIV='refresh' CONTENT='0'>";
    ?>
    <title>Home</title>
    <link rel="preconnect" href="https://fonts.googleapis.com">
    <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
    <link href="https://fonts.googleapis.com/css2?family=Rubik:wght@400;500&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="/assets/css/style.css" />
</head>
<body>
    <div class="container">
        <div class="sidebar">
           <div class="logo"> <img src="/assets/images/logoPGDF.png" /></div>
        </div>
        <div class="content">
            <nav>               
                <a href="{{route('home_op')}}" class="btn btn-primary">Home</a>
                <a href="{{route('user.create')}}" class="btn btn-primary">Trocar senha</a>                
                <a href="{{route('working_hour.create')}}" class="btn btn-primary">Expediente</a>
                <a href="{{route('record.create')}}" class="btn btn-primary">Registrar</a> 
                <a href="{{route('logout')}}" class="btn btn-primary"> Sair  </a>
            </nav>

            <main>
                <section class="register">
                    {{-- <span class="primary label"> Seja Bem-vindo(a): {{$AuthUser->name}}</span>         --}}
                 </section>                                                   
            </main>
        </div>
    </div>    
</body>
</html>